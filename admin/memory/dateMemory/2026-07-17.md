# 2026-07-17 问题台账更新

## 总部端改动

### 页面
- `views/issue/IssueList.vue` — 问题台账列表+详情抽屉
  - 状态筛选：PENDING_CONFIRMATION/IN_PROGRESS/PENDING_ACCEPTANCE/CLOSED/OVERDUE
  - 严重程度标签：严重(红)/较大(橙)/一般(蓝)/轻微(灰)
  - export 功能已移除

### 路由
- `/issue` → IssueLedger，角色：headquarters_admin, operation_admin

### 菜单
- App.vue 中"问题处理"指向 `/issue`，在"门店运营"分组下

### 侧边栏菜单优化
- "问题处理"、"物流信息"、"督导拜访"暂隐藏（问题处理后续恢复）
- 菜单项注释保留，需要时取消注释即可恢复

### 问题台账 IssueList.vue 优化
- **列表列**:去问题编号、上报人、象目单号；门店列 fixed:left、操作列 fixed:right
- **紧急程度**:标签半透明填充+圆角，颜色按关键字匹配(严重红/较大橙/一般琥珀/轻微灰)
- **搜索区**:一行4个(md=6)，下拉选项值改为库内原始值
- **详情抽屉**:两卡片合并为"基本信息"，字段：问题编号+门店+标题+问题类型·子类型+严重程度(标签)+处理人+提交时间
- **问题编号**：展示在顶部状态标签下方

### 支出统计 ExpenseDashboard.vue 优化
- **时间选择**:a-segmented→a-range-picker 日期范围选择器
- **门店对比**:新增金额/次数双维度切换(a-radio-group)+升序/降序排序按钮；列表可滚动
- **饼图**:donut 改为动态 conic-gradient，用真实数据渲染
- **月度趋势**:接后端真实数据；后端新增 MonthlyTrend DTO+Service

### 调货台账 TransferList.vue 优化
- 流程记录去"已发货/已交接"步骤
- 物料明细展示金额(数量×单价)+总金额；去调货编号列；门店列加宽
- 物料名 flex:1 占满宽度，数量靠右

### 报损管理 LossList.vue 优化
- 附件图片改为 a-image-preview-group 点击预览
- 去业务编码列，门店列加宽
- 修复到货报损数量显示(字段名 lossQty→inputQty)

### 员工统计 EmployeeDashboard.vue 优化
- 各门店人员分布可滚动(max-height+overflow-y:auto)

### 后端 ExpenseServiceImpl 改动
- dashboard 接口支持 startDate/endDate 参数
- StoreRanking 新增 count/pctAmount/pctCount
- 新增 buildMonthlyTrend 按月汇总
- buildStoreRanking 去 limit(8) 返回全部门店
