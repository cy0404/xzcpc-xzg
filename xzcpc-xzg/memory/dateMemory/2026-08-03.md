# 2026-08-03 盘点差异处理阶段性完成

## 完成状态
盘点差异处理模块已完成核心开发，数据已清除，等待定时任务重新计算。

## 关键决策
1. **时间窗口**：不再用 `task_month`，改为 `上次submitted_at → 本次submitted_at`
2. **PG 查询**：`stat_date` 按天，NULL unit 用窗口函数补全
3. **计算时机**：每月1号10:00 定时任务 + 批量接口手动触发
4. **单位换算**：调货/还货/自购/消耗统一走 `convertToBaseUnit`
5. **消耗为0过滤**：不在差异列表中展示

## 待处理
- 当前生产数据已清，批量接口 `POST /api/admin/inventory/diff-tasks/batch-calculate` 待调用
- 7月20号后提交的任务在列表可见
