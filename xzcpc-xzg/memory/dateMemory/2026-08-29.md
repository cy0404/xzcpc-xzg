# 2026-08-29 补发联动企迈出库单 + 换算失败可重试 + 统计报表督导视图 + 评价管理渠道化/督导范围 + 物料同步增强

## 背景

飞书 H5 补发页点「补发」后联动企迈 9.2.4 创建出库单（自动扣库存）。此前 convertQty 的 SQL 写错（列名不存在 + 按 material_id 直查）→ 异常被 catch 吞掉 → **降级按原数量建单** → 7 张出库单数量错 24 倍/16 倍/12 倍/5 倍。本次修复换算 + 废除降级 + 支持失败重试。

## 一、出库单联动（口径定稿，用户两次纠正后强制）

- **数量**：`num` 按**库存单位**（`stock_unit`，回退 `purchase_unit`），报损数量经 `material_conversion_rule` 换算链换算
- **单价**：`price` = **采购单价 `purchase_price`**（material_inventory_rule）
- ❌ 绝不使用 `order_unit`/`order_price`（用户原话：「补发，单位是用库存单位，单价是采购单价！」）
- **接口**：企迈 9.2.4 `POST /v3/scm/order/outbound/order/create`；productList 无单位字段；`autoOutbound=1` 立即扣库存；warehouseNo 用 **PSCK 格式编码**（非控制台数字 ID）
- **幂等**：`externalNo = BF{base}-{seq}`（base=首条 id，seq 按仓库分组序号；失败占位 `BF{id}-0`），`outbound_order.external_no` 唯一键，`insertOutboundOrder` upsert（先 UPDATE 后 INSERT）

### 仓库映射（resolveOutboundWarehouse）

| 条件 | 仓库 |
|------|------|
| 分类=冷冻类 或 名称含 FROZEN_MATERIAL_KEYS（安佳淡奶油/酸奶/咸法干酪乳/牛油果泥） | 冷冻仓 `PSCK000149` |
| 分类=鲜奶类 | 鲜奶仓 `PSCK000070` |
| 其余（固体类/茶叶类/液体类…） | 总仓 `PSCK000023` |

仓库编码存 sys_config：`loss_outbound_wh_frozen/fresh_milk/central`。

## 二、convertQty 换算（SQL 修复 + 失败语义变更）

- **SQL 修复**：`material_conversion_rule` 表**没有 material_id 列**！必须 `JOIN material_inventory_rule ir ON ir.rule_id = c.rule_id WHERE ir.material_id=?`；列名是 `from_quantity/to_quantity`（不是 from_qty/to_qty）。旧 SQL 每次执行抛 Unknown column → catch 吞掉 → 降级建单
- **换算算法**：`ConversionFactorUtil.computeConversionFactor(fromUnit, toUnit, rules)` 双向 BFS（每条边双向 ratio，仅 unit 类型），如 1包=250g、1件=6000g → 包→件 = 1/24
- **换算失败新语义（本次核心）**：**不再降级按原数量建单**！
  - 单位缺失 / 无换算链 / 换算异常 → `convertQty` 返回 **null**
  - `createOutboundForLoss` 收到 null → `markOutboundFailed(r, wh, "物料缺少换算链（input_unit → stock_unit）…")` + continue（不进建单分组，**绝不带错误数量建单**）
  - `markOutboundFailed`：写 `BF{id}-0` failed 占位（含仓库编码 + error 原因）+ UPDATE loss_report.outbound_order_id + `sendOutboundAlert` 飞书告警（sys_config `loss_outbound_alert_chat_id` 有值只发该群，否则发其他类补发收件人）
- **门店补发不受影响**：`issueVoucher`/`batchIssueVoucher` 里 `status='confirmed_resend'` 的 UPDATE **先于** `createOutboundForLoss` 执行（独立语句）——换算失败只标记出库，本地状态照常流转，小程序收货流程照常
- **重试闭环**：H5 已补发 tab 失败行显示红行（⚠ 出库失败 + error）+ 「重新补发」按钮 → `POST /api/public/loss-report/retry-outbound {id}` → 重新走 createOutboundForLoss：成功 → `BF{id}-1` 新单（upsert 覆盖无冲突）；再失败 → 覆盖 `BF{id}-0` failed 记录 + 再告警
- **H5 判断**：列表接口返回 `outbound_status`（JOIN outbound_order o，o.status AS outbound_status），前端 `outboundStatus === 'failed'` 显示红行 + 重试按钮；tab 切换真实模式 `location.reload()`（localStorage activeTab_<cardKey>），**TEST_MODE（URL test=1）前端模拟不 reload**（testShowDoneTab/testShowPendingTab）

## 三、7 张历史错误出库单（数据处置）

错建原因：旧代码换算降级，报损数量（包/瓶）按「件/份」直接建单。正确数量 = 报损数量 × factor（换算链）：

| 记录# | 门店 | 物料 | 报损 | 错建 | 正确出库 | 正确金额 |
|------|------|------|------|------|---------|---------|
| 9704 | 安宁金色时代店 | 牛油果泥 | 1包 | 1件 | 0.0417件(1/24) | 12.00 |
| 11446 | 昆明财大龙泉路店 | 牛油果泥 | 13包 | 13件 | 0.5417件(13/24) | 156.00 |
| 15144 | 昆明财大龙泉路店 | 牛油果泥 | 4包 | 4件 | 0.1667件(1/6) | 48.00 |
| 14981 | 昭通金池店 | 牛油果泥 | 25包 | 25件 | 1.0417件(25/24) | 300.00 |
| 15003 | 昭通金池店 | 牛油果泥 | 27包 | 27件 | 1.125件(9/8) | 324.00 |
| 16496 | 黔西南兴义梦乐城店 | 珍珠粉圆 | 1包 | 1件 | 0.0625件(1/16) | 8.50 |
| 16624 | 玉溪华宁盘溪镇店 | 普洱茶 | 2包 | 2份 | 0.2份 | 15.00 |
| 18186 | 东川店 | 厚椰乳 | 1瓶 | 1件 | 0.0833件(1/12) | 8.30 |

- 错误金额合计 **20,545.60 元**，正确合计 **871.80 元**（差额 19,673.80 = 企迈多扣库存）
- 11446+15144 合并成一张企迈单 `BF15144-1`（错建 17件），所以是 **7 张企迈单**；15575（牛油果泥 2包）此前因未配采购单价已 failed（现单价 288 已配好，可直接重试，无需 SQL）
- **处置**：`database/fix-7-bad-outbound.sql`（8 条记录 → 7 张 outbound_order status=success→failed + error 说明），执行后 H5 已补发 tab 变红行，点「重新补发」按正确数量建**新单**
- ⚠️ **企迈旧 7 张错单已在企迈建单成功（扣库存），API 无撤销接口，需仓配中心人工作废/回冲**——重新补发后新旧两单并存

## 四、启动失败修复（部署 server 时踩坑）

新 jar 启动报 `IssueFeedbackServiceImpl constructor parameter 3 ... No qualifying bean of type Executor ... found 3: logTaskExecutor, alertTaskExecutor, taskScheduler`。两个叠加根因：

1. **m2 依赖 jar 落后于工作区**：工作区大量未提交改动（8/28-29），本地 m2 的 common jar 是 8/11 旧版（无 `feedbackNotifyExecutor` bean）。`mvn package -pl server`（不带 -am）引用 m2 旧 jar → 新 jar 里 common 是旧的
   - **修复**：`mvn install -pl common,template,task,expense,people -DskipTests`（排除编译不过的 mp）→ 重新 `mvn package -pl server`
2. **Lombok 构造注入丢 @Qualifier**：`@Qualifier("feedbackNotifyExecutor") private final Executor x;` + `@RequiredArgsConstructor`——Lombok 生成的构造参数**不带字段上的 @Qualifier**，编译又无 `-parameters`（pom 未配）→ Spring 按类型匹配 Executor 命中多个
   - **修复**：改 `@Autowired @Qualifier(...)` **字段注入**（去掉 final，Lombok 构造器不再含该参数；@Qualifier 在字段上生效）
   - 全项目排查：仅 IssueFeedbackServiceImpl 一处（新文件，未提交）

## 五、mp 编译错误（已修复）

- **mp 之前的「找不到符号」**（MpTaskServiceImpl:288/737/738、SmartOrder:124/125/149/150/172/519/2005）：m2 旧 task jar 缺新实体方法（getTaskType 等），install 新 task 后**自动消失**（不是工作区代码问题）
- **唯一真错误**：`SmartOrderServiceImpl.java:372` —— `Task.getId()` 返回 `Integer`（Task.id 是 Integer），`persistOrder(..., Long taskId, ...)` 参数要 Long → 改 `.longValue()`
- 修复后**全量 `mvn clean package -DskipTests` 通过**，server + mp-server 两个 jar 均为全模块最新代码（此前 server jar 的 mp 部分是 8/11 旧 jar）

## 六、部署清单（给用户）

1. 部署 `server/target/store_inventory-1.0.0.jar`（总部 4026）——**先重启再用新 jar**
2. 执行 `database/fix-7-bad-outbound.sql`
3. H5 补发页（**不带 test=1**）→ 已补发 tab → 8 行红行 → 逐个「重新补发」
4. 企迈旧 7 张错单人工处理（仓配中心）
5. mp-server（30261）如需更新用新 jar

---

## 七、统计报表督导视图（TaskList.vue 报表 Tab）

- **督导筛选 + 自动锁定**：总部下拉选督导（reportSupervisor → 后端 supervisorName 参数）；督导登录**自动锁定本人视图**——前端 `isSupervisorOnlyRole()`（auth.ts，语义对齐后端 AdminRole.isSupervisorOnly）判断，后端 page() 按 openId 强制 store 过滤（前端不用传姓名）
- **状态饼图**：零图表库，纯 CSS `conic-gradient` donut（白孔 + 中心门店总数）；三状态 chips（未开始 #98A19C / 进行中 #5B9BC7 / 已提交 #2F8F57，项目色板低饱和）**点击切换门店列表**
- **门店列表**：默认「未开始」优先（按 deadline 排序；submitted 按门店名排序）；每行 = 门店名 + **督导列**（蓝底标签）+ 截止时间/任务数；点击状态 chips 切换
- **督导完成度卡**：仅总部全部视图显示（`v-if` 非锁定非选择），按督导分组进度条（复用 .report-item/.bar-seg 样式）
- **数据流**：fetchReport 单次 `getTasks`（taskType+taskMonth+supervisorName，pageSize 10000）聚合指标/饼图/门店分组/督导完成度；原 fetchStats 删除；月份为空 → 空态
- **布局坑（重要，踩过）**：卡片内容区固定 340px 高 + 内部滚动。ant-design-vue `a-row/a-col` 根元素**不携带父组件 scoped data-v** → `.report-body-row/.report-col` scoped 样式不命中 → height/flex 失效 → 门店多时列表溢出视口（截图排查才发现）。**修复：改纯 div flex 布局**——`.report-body-row{display:flex;gap:24px;height:340px}` + `.report-col--donut{flex:0 0 330px}`（三个 chips 一行需 ~295px）+ `.report-col--store{flex:1;min-width:0}` + `.store-list{flex:1;min-height:0;overflow-y:auto}`；另加 768px 窄屏媒体查询纵向堆叠防御
- 配套：api/task.ts getTasks 参数补 `storeIds/supervisorName`

## 八、评价管理渠道化 + 督导数据范围（客诉反馈）

- **渠道字段**：`issue_feedback` 加 `channel`（默认 'scan'；未来美团/小红书平台差评预留 meituan/xiaohongshu），迁移脚本 `database/migration-add-feedback-channel.sql`（**待用户执行**）；`submit()` 渠道空默认 scan
- **管理端**：列表按渠道筛选 + Excel 导出（表头含「渠道」，channelText 中文映射）；导航入口「问题反馈」→「**评价管理**」
- **督导数据范围（重要）**：普通督导（supervisor_admin）在评价管理**只能查看/处理自己负责门店**（supervisor_store_access 映射）的反馈——`storeScope()` 返回门店列表 → `adminPage` 按 store_id in 过滤；详情/改状态有 `checkStoreScope` 越权校验；督导未配置门店 → 空页/导出空表；总部/运营全量
- **入口开放**：App.vue 导航 + router roles + canSeeFeedback 均加入 supervisor_admin

## 九、物料同步增强（MaterialSyncServiceImpl）

- **源集合改按 qm_code 匹配**（原按 material_id）：接口 id 会随上游重建**更换体系**（新旧两套并存），deleteAbsent 按 material_id 匹配会把「接口仍存在但 id 已换」的存量物料误删 → `deleteAbsentMaterials(sourceCodes)`；规则归属存量行 material_id（`upsertRules(RuleSource.of(existing.getMaterialId(), m))`），避免孤儿规则/存量规则更新不到
- **半成品 semi-enabled=false 时仍拉接口**（仅保护 + del_flag 归位）：不插新、不建规则、不改字段；按接口状态 `updateDelFlagOnly`（ENABLED→0 复活误删残留、DISABLED→1），qm_code 命中任意 del_flag（0/1/2）都覆盖；拉取失败同样抛异常中止——保护集合不全宁可整轮不跑也不误删
- **新增 `database/migration-sync-material-prod-to-test.sql`**：生产库→测试库三表（material/rule/conversion）全量镜像，先删后插保 id + 修正 AUTO_INCREMENT 游标；执行前建议先跑一次生产 xinfo 同步
