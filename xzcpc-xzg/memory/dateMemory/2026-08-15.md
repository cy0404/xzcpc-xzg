# 2026-08-15 会话日志

## 飞书内置浏览器媒体不显示排查（图片/视频）

- 现象：飞书桌面客户端内置浏览器打开审核页，图片/视频不显示；外部 Chrome 正常；图片直链在飞书聊天里点开正常显示；9:40 重新部署过 server jar 后出现
- 排查路径：nginx -T 无 valid_referers / Content-Disposition（只有 Cache-Control + CORS 头）→ 排除防盗链；页面无 CSP、无 loading=lazy → 排除页面机制；媒体 CSS 新旧版一致（aspect-ratio 老版就有）→ 排除 CSS 回归
- 服务器日志线索：`客户端断开连接 - GET /api/public/loss-report/daily-summary`（09:52:48）+ `GET /loss-daily-confirm.html` ×2（09:55:01/02，1.4 秒内两次）——ClientAbortException，飞书 webview 掐断请求；其后 GlobalExceptionHandler 二次报错（HttpMessageNotWritableException: No converter for R with preset Content-Type 'text/html'）是异常处理器向已断开连接写 JSON 失败，**纯噪音无害**
- 实锤问题：新审核页每个视频 `poster=<原名>_thumb.jpg`，但生产 mp-server 仍是 8月10 老 jar（generateThumbnail 是 8/11 ee58cc8 才提交）→ 线上所有视频没有 _thumb.jpg → poster 全部 404
- 修复：视频块 `onerror="this.removeAttribute('poster')"` 降级（黑块+播放键），loss-daily-confirm.html，node --check 通过，随 server 打包生效
- 弯路教训：用户两次"打包上传"的 server jar 都不含本地改动（线上 curl grep removeAttribute=0）——打包用的代码副本与工作区不是同一份；给出 Select-String 验证法（打包目录里 `server\src\main\resources\static\loss-daily-confirm.html` 含 removeAttribute 计数应为 1）；admin vite outDir=dist 不覆盖 server static（排除 npm build 清空嫌疑）
- 最终：图片恢复（用户确认），视频慢 = faststart 老问题（见下）

## 存量视频 faststart（用户拍板：只处理 13 号之后的）

- 背景：8/13 compress-videos.sh 重编码 34/34（自带 faststart，点击快）；8/13 之后新上传的视频是原片（生产 mp-server 8月10 无 faststart 代码），点击慢
- 用户决定：「视频处理13号之后的就可以了」+ 追加「不含8月13日」→ faststart-all.sh 加 `SINCE_DATE` 变量（默认 2026-08-14，env 可覆盖）+ find 加 `-newermt "$SINCE_DATE"`；bash -n 通过
- **🎯 测试环境 ffmpeg 每次失败根因终于实锤**（首轮跑脚本 10 个失败日志）：`Unable to find a suitable output format for '...mp4.faststart.tmp'`——ffmpeg 靠输出扩展名推断容器格式，`.faststart.tmp` 猜不出 → 报错。**Java 端 faststartRemux 同款坑**（tmp 后缀同名）→ 测试环境上传每次失败原因即此，不是磁盘/编解码
- 修复两处：脚本 `-f "$fmt"`（mov→mov，其余 mp4）+ MpUploadController.faststartRemux 加 `-f fmt`（name.endsWith(".mov")?"mov":"mp4"）；compress-videos.sh 之所以一直成功是因为临时文件后缀正常
- 首轮统计：共66个 重排0 跳过56（压缩脚本处理过的 8/14 文件，已是 faststart 自动跳过）失败10（8/14 后新上传原片，即点击慢的那批，修完重跑会全部成功）
- 部署位置：/data/inventory-upload/ **根目录**（与 voucher/、h5/ 同级）——不放 h5/（外网可下载 + 脚本找不到 voucher 目录）；UPLOAD_DIR/LOG_FILE env 可覆盖
- 执行：chmod +x → ./faststart-all.sh → tail -20 /data/logs/faststart-all.log；MIN_AGE 60 分钟保护，最近一小时上传的需一小时后重跑（幂等）
- 日志含义：跳过(已是faststart)=压缩过的；完成=13号后原片已重排

## 第二轮：视频仍慢 —— 点视频零请求 + tile 去 video 化

- 用户实测（10:38-10:39 nginx 日志）：挂 tail 点视频，日志只出现 4 张曲靖万达店 jpeg（200 正常返回，5-6MB/张），**一条 .mp4/.mov 请求都没有** → 客户端压根没向服务器要视频文件（慢的不是下载）
- 线上核对：curl http://162.14.122.80:4026/loss-daily-confirm.html 与本地 diff=0 —— 上次部署终于到位（preload="none"/poster 降级/thumbOf 都在）；域名根路径 /loss-daily-confirm.html 404（页面由 4026 jar 直连提供）
- 用户观察「页面把所有视频加载完才开始播放」→ 飞书 webview（UA: Lark/7.74.16 Electron/Native WebApp）无视 preload="none"，进页面为每个 `<video>` 拉全部视频数据，大文件排队下载 → 点击慢
- 修复（loss-daily-confirm.html）：tile 内 `<video src poster preload="none">` 换成 `<img src=thumbOf(mediaUrl) onerror="this.remove()">` —— 页面不再有任何带 src 的 `<video>`，任何浏览器都不会预拉视频字节；点击才给弹窗 #videoPlayer 设 src。老视频无 _thumb.jpg → onerror 移除 img → 黑块+▶（与 poster 降级等效）。CSS `.photo-video video` → `.photo-video img`。node --check 通过
- loss-arrival.html（upload/h5）核实：本来就是 img 缩略图 + 点击动态建 video 模式，无需改
- 打包验证标记：`this.remove()` 计数=1（Select-String，防打包副本不一致再次发生）
- 附带发现：nginx 媒体 `Cache-Control: public, immutable` + faststart 原地重写文件 = 已缓存旧文件的客户端永不发新请求；建议 nginx 改 `public, max-age=86400` + 飞书清缓存（头像→设置→通用→清理缓存）

## 智能订货测试环境部署 + 测试门店种子

- 404 定位：小程序 ENV=test → 点智能订货打开 `https://www.xzcpc-9pd.top/test/storeInventory/upload/h5/smart-order-list.html`，curl 探测 404 而 inbound-list 200 → 测试服务器 `/home/StoreInventory/test/mq-server/upload/h5/` 缺两个新文件（用户已传，探测转 200）
- 顺带发现：生产 `/storeInventory/upload/h5/inbound-list.html` 也 404（生产目录缺入库两页），切 prod 构建后入库入口会 404，上线时一起补传
- 测试环境后端已部署新 jar（/api/mp/smart-order/overview-total 探测 200）；测试库 = `store_inventory_test`（162.14.122.80，独立于生产 store_inventory）
- `POST /api/mp/smart-order/generate` 无 storeId 参数，generateAll 遍历全部门店（有已提交任务的店都会生成，幂等按 store+week）——计划里"可指定门店"没实现，联调可考虑补参数
- 新建 `tmp_smart_order_seed_test_store.sql`：只给「测试门店」插 1 张 pending 建议单；suggest 为测试值；带幂等 guard（本周已有单据/明细则跳过）+ 诊断 + 清理语句
- 用户反馈「这个店没有盘点数据」→ 明细改为**三级降级数据源**：A 最近已提交任务汇总（库存×25% 最低1，与真实生成同源）→ B store_zone_material 门店默认分区清单（suggest 固定3）→ C material 主数据有 qm_code 前10条（suggest 固定3）；诊断 0.2 可看到哪级有数据（注意测试库测试门店 id≠1，不能硬编码）
- ⚠️ H5 点「确认订货」会真实调企迈 createDeclareOrder（测试门店也真下单）；确认需 cangkuid 非空（否则自愈读近 60 天报货单，仍无则报错）
- **storeId 语义（全系统一致，易踩坑）**：`StoreServiceImpl.toStoreInfo` 里 `info.setId(s.getStoreId())` → 业务上下文的 storeId = `store_info.store_id`（外部API门店ID），不是自增主键 id；task/employee/store_zone_material/smart_order 的 store_id 全是外部 ID。resolveWarehouseNo 的 `WHERE store_id=?` 无 bug；但种子脚本最初误用 CAST(id AS CHAR)，会导致 H5 列表查不到种子单（已修为 store_id）。测试门店 cangkuid/qmai_store_id 若在测试库为空，需从生产库拷贝回填（脚本 0.1b 有模板）
- **测试库 schema 滞后**：跑种子脚本报 `1146 store_zone_material doesn't exist` → 新建 `tmp_test_db_smart_order_schema.sql`（幂等补表：store_zone_material、task_material_summary（含 V9 original_qty/adjusted_qty）、smart_order、smart_order_item、sys_config + V17 5 条配置；指向既有表的外键不建，避免测试库字符集不一致报 errno 150）。若补完后种子 C 级（material 主数据）仍为空，下一步需从生产库拷物料三表（material/material_inventory_rule/material_conversion_rule）

## 第三轮：图片缩略图治本（用户拍板"改"，已实现待打包）

- 用户先问「缩略图不会影响原图吧」→ 不会：只新增 _thumb.jpg 新文件，原图 URL/字节不动；再问「图片很多地方都在用、报损集中在晚上10点，生成缩略图不会有影响吗」
- 解答与落地：**上传时生成走异步后台线程**（generateImageThumb，new Thread 模式同 transcodeToH264）——上传响应立即返回，缩略图晚 1-2 秒出现，晚 10 点高峰上传速度零影响；审核页第二天才看，缩略图早已就绪；失败静默忽略（页面 onerror 回退原图）。**存量批量脚本放凌晨跑**（crontab 0 2 * * *，nice -n 10 串行），与晚 10 点完全错开。其他用图处（大图弹窗/ZIP 下载/Excel 导出/飞书卡片/到货登记页）原 URL 不变不受影响
- 改动三处：
  1. MpUploadController.generateImageThumb（新增，ffmpeg `-y -nostdin -v error -i src -vf scale=320:-2 -frames:v 1 -q:v 5 <名>_thumb.jpg`，异步线程；uploadVoucher + completeChunkUpload 图片分支各挂一处）——命名与页面 thumbOf() 同规则（lastIndexOf('.') 截断 + _thumb.jpg）；视频缩略图（generateThumbnail 同步）不动
  2. loss-daily-confirm.html 图片 tile：`src=thumbOf(mediaUrl)` + `data-orig=原图URL` + onerror 一次性回退（`if(this.src!==o){this.src=o}`，回退后原图再失败不再循环）；点击 showLightbox 仍看原图。node --check 通过
  3. upload/generate-image-thumbs.sh（新脚本）：find voucher 下 jpg/jpeg/png/gif/bmp/webp，已有 _thumb.jpg 跳过（幂等）、60 分钟保护、tmp+mv 防半截文件、flock 单实例、nice -n 10 串行；部署 /data/inventory-upload/ 根目录（与 faststart-all.sh 同级）
- **打包验证标记**：server jar —— loading="lazy"=2 + this.remove()=1 + **data-orig=3**（注释+属性+getAttribute 各一次）；mp-server —— faststartRemux=3 + **generateImageThumb=3**（方法+两处调用）
- gif 只取第一帧、png 透明底会变黑底（可接受）；ffmpeg autorotate 自动按 EXIF 转正方向
- **首轮实跑失败（14:10，北城天地店/东川店一批全失败）**：`Unable to find a suitable output format for '..._thumb.jpg.tmp'`——又是输出扩展名推断格式的坑（.tmp 后缀，与 .faststart.tmp 同款，自己踩自己修过的坑）；修复：ffmpeg 加 `-f image2` 显式指定单帧图片输出；Java 端 generateImageThumb 直接写 .jpg 结尾无此问题；失败的都是先 rm 后重试，幂等重跑即可
- **⚠️ 教训沉淀为通用规则**：服务器 ffmpeg 3.4.13 任何输出文件带 .tmp 等未知后缀都必须显式 `-f`（faststart .faststart.tmp→-f mp4/mov；图片 _thumb.jpg.tmp→-f image2）
- **重跑成功（14:17）**：共4125个 生成4125 跳过0 失败0——存量图片（不止当天 203 张，含历史全部）缩略图已全部生成；磁盘就绪，只等 server jar 部署后审核页切换生效。**14:59 再跑：跳过4125/4125 幂等验证通过**
- **点击图片弹窗慢（后续反馈）**：弹窗 showLightbox 直接加载原图 5-6MB ≈ 9 秒白屏等待（5Mbps 物理上限）→ 改为**渐进式**：先显示缩略图（tile 已缓存秒出，用户要求**不加模糊效果**）+「高清图加载中…」提示，原图后台 new Image 加载完自动替换（onerror 缺失缩略图回退原图直载，双重防循环）；新增打包验证标记 **lb-tip=4 + 高清图加载中=1**；物理等待时间不变（原图还是要 9 秒），观感从白屏变秒出渐清晰。可选后续治本：_mid.jpg 中尺寸图（1080 宽 ~150KB）

## 牛油果泥审核页：厂家单独一行

- 需求：备注是「厂家：蓝蛙牛油果 变质之前说够寄回去现在视频报损」这种门店手打格式；牛油果泥审核页要把厂家单独一行显示
- 后端早已支持无需改：dailySummary 里 `extractSupplier` 正则 `厂家[:：]([^，,\s]{1,30})` 从 remark 解析 supplier，对所有报表（含牛油果泥）都返回
- 页面改动（loss-daily-confirm.html，纯前端）：① 卡片厂家行去掉 `!r.isAvocado` 条件（原 743 行）② 备注渲染 else 分支：r.supplier 存在且备注以「厂家：X」/「厂家:X」开头 → indexOf+substring 剥离前缀再显示（避免厂家出现两遍；全角/半角冒号都处理）③ 统计表 sg-meta 厂家徽标同样去掉 `!r.isAvocado`（原 399 行）④ 用户补充「厂家用标签包起来」→ 三处厂家值全改用页面标准 tag() 药丸标签（新增 `.tag-supplier` 绿色类，与 tag-ok 同色），不再是手写内联 span
- 打包验证标记新增：**`indexOf('厂家`=2 + `'supplier')`=3 + `.tag-supplier`=1**；`!r.isAvocado` 剩 3 处均为正常用途（【】分支 + otherList/resendOther 过滤）
- **用户追加「补发卡片里不要那个展示厂家」**：删掉 buildStoreGroups sg-item 行（sg-meta）的厂家药丸标签——buildStoreGroups 仅补发卡片（renderResendCard）使用，审核/确认页卡片不受影响；标记更新 `'supplier')`=3→**2**（只剩审核页卡片厂家行 + 【】备注行两处）
- **用户反馈「之前没有选厂家的下载到蓝蛙牛油果泥的zip里」**（无厂家报表被混进蓝蛙 ZIP）：根因是「空 supplier→蓝蛙」兜底（到货登记页厂家选择写的是 `【蓝蛙牛油果】` 前缀，extractSupplier 只识别 `厂家：X`，识别不到的新旧报表全兜底成蓝蛙）。修复：① 后端 extractSupplier 增加解析 `【(.+?)】` 前缀（登记页格式，hass牛油果 从此不会再被错归蓝蛙）② 4 处「蓝蛙」兜底改「无厂家」独立分组（supplierSet / 下载配套 Excel 厂家列 / exportAvocado 过滤 / ZIP 内文件夹 `无厂家/`）③ 页面 doDownload 分组兜底改「无厂家」（单独出一个 ZIP）+ 备注渲染同步剥离「【supplier】」前缀防双标签 + 【】分支加 `!r.supplier` 条件防重复标签。打包验证标记：页面 `'无厂家'`=1 + 页面「蓝蛙」=0；Java `无厂家/`=1 + `Pattern.compile("^【(.+?)】")`=1

## 入库管理接入企迈 OpenAPI（方案B 全 OpenAPI 单通道，已实现待部署）

- 决策：9.2.3 实测成功（code=0，warehouseNo=cangkuid 与 warehouseMark 均可用，子集物品也接受）→ 9.2.2 批量查询入库单（租户级、含明细、无 total 按页<pageSize 判尾）→ 用户拍板「只需要1仓配入库、3采购 选B」：控制台接口+Cookie 自动登录不用做
- 同步重写：syncFromQmai(storeId, qmaiStoreId, days) — 报货单归属键集 → 9.2.2 全量分页 → 类型 {1,3} + bizNo 匹配 → inbound_no upsert（只覆盖企迈侧字段；pending 才随状态联动；无明细时回填）
- 收货：三接口本地更新后同事务调 9.2.3；失败 FeishuWebhookAlertService 告警 + BusinessException 回滚；operator=storeName；price 兜底 "0"；productCode 空直接报错
- 新文件 InboundSyncJob（凌晨3点，近7天）+ InventoryMpApplication @EnableScheduling；controller sync 去 warehouseId 校验、days=2；QmaiClient 加 9.2.2/9.2.3 方法
- 编译通过（mvn -pl mp -am compile）；待部署：测试库重建 inbound 表、mp-server 打包、生产 upload/h5 补 inbound 两页
- **实测企迈无自动入库**（tmp_qmai_inbound_status.py，9.2.2 近7天）：类型1/3 待入库 879 vs 已入库 644；已入库 92% 是创建 3 天后人工确认（只有 21 条 1 小时内，总仓库管即时收）；待入库最老 8/8（有 7/26 采购单对应单据）。用户问「入库单不是会自动入库吗」→ 给数据后**拍板：不加超时自动入库，纯手动确认**
- **用户问「现在怎么批量把入库单存到我的数据库」**→ 生产库 inbound 两表已被删（空）；方案：① 跑 migration-add-inbound-order.sql 重建 ② tmp_inbound_backfill.py 生成幂等回填 SQL（默认 30 天入库 + 60 天报货匹配窗口）用户执行 ③ 后端部署后增量接管
- **🎯 9.2.3 price 参数实测不生效**（tmp_qmai_price_unit_test.py，测试门店待入库单写 12.34 回读仍 1200 分，price 无副作用）但 status 1→2 确认收货生效（测试门店那笔 8/12 仓配单在企迈被置已入库，无碍）；9.2.2 金额/单价单位为分 → Java `fenToYuan` ÷100 存元 + 匹配窗口 max(days,30)；重编译通过
- **9.2.3 已入库单写入静默不生效**（晚 17:32 复测 tmp_qmai_price_unit_verify2.py，对同一张 status=2 单写 12.34/12.00 均 code=0 但回读恒 1200 分）—— 已入库单不可再改，9.2.3 仅对 status=1 单有意义（num 生效 + 翻状态）；靶子单终态原值未动
- **部分收货语义修正（用户纠正 + 官方文档佐证，8/15 晚）**：9.2.3 官方定义为"收货过程中更新实际入库数量（productList[].num）"，参数表**无 status**；另有 **9.2.9 关闭入库单**（inbound/order/finish，inboundNoList）。官方流程：9.2.1 create(autoInbound=0) → 9.2.3 录入数量 → 9.2.9 完成收货关单。用户结论：部分收货=写 num=2 数量变 2、状态保持 1，收齐才翻 2（以前只是没人操作过部分入库）。下午实验传的是全量数量故翻 2，**不能证明"一调即整单"**。数据扫描（18203 条）只能说明无人用过部分收货，不能排除该能力
- **待实测（直接影响 receiveItem 传参语义）**：① num=部分量时 status 是否保持 1、9.2.2 回读 inboundNum 是 2 还是 12 ② num 是"本次实收"还是"覆盖累计"——若是覆盖语义，当前每次收货传 receivedQty 会把前次覆盖，必须传累计值 ③ 9.2.9 是否必须调（全量时疑似自动关单）。**实测方案已定（用户拍板"等我建一个单子"）**：用户自己在企迈控制台给测试门店建一张多数量待入库单，脚本 `tmp_qmai_partial_receive_test.py` 已备好（列表模式只读验证通过 8/15 晚），拿到单号即跑：写 partial=2 → 回读 → 写全量 → 回读 → 必要时 9.2.9

## 智能订货：库存单位换算 + 整数数量（用户拍板，已实现待部署）

- 用户反馈：`单位应该换算成库存单位，价格也是。然后不应该有小数，因为仓库不好发货`；同时要求删除 H5 详情页顶部「智能订货」标题（header-title span 已删 + `<title>` 改「订货单」）
- 单价语义确认：`material_inventory_rule.unit_price` 是**基础单位单价**（MpTransferController 调货按 `unitPrice × factor` 出各单位单价；MpTaskServiceImpl 盘点金额 = baseQty × unitPrice）→ 智能订货按库存单位下单必须 `unitPrice × factor(库存→基础)`
- SmartOrderServiceImpl.generateForStore 改动：
  1. factor（库存单位→基础单位）提前计算；unitPrice = rule.unitPrice × factor（保留2位）→ **每库存单位单价**
  2. suggest_qty：无论有无换算链，统一 `setScale(0, CEILING)` **向上取整为整数**（原逻辑无链时保留2位小数，已删）
  3. reason 文案「按整箱规格向上取整」→「按库存单位换算并向上取整」
  4. confirm() 新增整数校验：qty 带小数 → 报「确认数量必须为整数（仓库按整件发货）」；confirmed_qty 落库 setScale(0)
- H5 smart-order-detail.html：qty 输入 `inputmode="numeric"`、onQtyInput/onQtyBlur/changeQty 全部 Math.floor 取整、单价 chip 显示「单价 ¥xx/单位」；node --check 通过
- 种子脚本 tmp_smart_order_seed_test_store.sql 同步：suggest_qty 整数（A 级 `GREATEST(1, CEIL(库存×0.25÷系数))`，B/C 固定3）、单价乘单级 unit 换算系数（**只支持单级链**，多级以真实生成为准）、B/C 级规则表改 INNER JOIN（无规则的物料不种）
- `mvn -pl mp -am compile` 通过（MAVEN_EXIT=0）；**待部署**：mp-server 重打包 + H5 详情页传测试服务器；若已用旧种子跑过，先执行脚本末尾清理段再重跑

## 待办清单

- [x] 重跑 faststart-all.sh：共66 重排10 跳过56 失败0（8/14+ 全部处理完）
- [x] 线上 4026 页面与本地 diff=0（poster 降级已上线）
- [ ] server jar 重新打包部署（tile 去 video 化 + 图片缩略图 tile + 弹窗渐进式缩略图 + 牛油果泥厂家单独一行；打包前验证 this.remove()=1 + loading="lazy"=2 + data-orig=3 + lb-tip=4 + 高清图加载中=1 + indexOf('厂家=2）
- [ ] 生产 mp-server 重新打包部署（faststart + 视频/图片缩略图 + ffmpeg 失败日志可见；低峰期重启 PID 14729；打包前 Select-String faststartRemux=3 + generateImageThumb=3）
- [x] generate-image-thumbs.sh 部署并跑完：共4125 生成4125 失败0（存量图片缩略图全部就绪）；crontab 0 2 * * * 兜底仍建议配
- [ ] nginx 媒体 Cache-Control 去掉 immutable（public, max-age=86400）+ 用户清飞书缓存
- [ ] faststart-all.sh 一小时后重跑（60 分钟保护内的新上传，幂等）
- [x] 测试环境 ffmpeg 失败根因：`.faststart.tmp` 扩展名推断不出格式（已修脚本+Java，加 -f）
- [ ] compress-videos.sh crontab（30 1 * * *）仍未配置

## 入库回填：四级归属匹配定稿（用户提醒「需要用 qmai_store_id」→ 实测定稿）

- 用户打断提「我记得是需要用qmai_store_id」→ 排查验证：
  - **9.2.2 忽略一切过滤参数**：storeIdList/storeIds/storeId/warehouseNo/warehouseCode/warehouseId/warehouseIdList 逐一实测全返回租户全量；入库单记录里也没有 storeId 字段 → 门店归属只能客户端匹配
  - **报货单（9.1.17，按 qmai_store_id 查）记录里有 storeWarehouseNo（=入库单 warehouseCode）+ purchaseNoList（CG* 采购单号）**——之前只收了 purchaseApplyNoList（CGSQ 采购申请），企迈两套编号互不相通（报货单挂 CGSQ，入库单挂 CG）→ 补收 purchaseNoList + 新增仓库编码集匹配
  - **一店可多仓库**：版纳曼城店两个仓 MDCK000152/154 → 匹配按「仓库编码集合」而非单个 cangkuid
- Java（MpInboundServiceImpl，编译通过）：
  - 同步四级匹配：① bizNo∈键集（含 purchaseNoList）② warehouseCode∈本店仓库集（storeWarehouseNo∪cangkuid）③ 归一化名称（去括号/空白）④ 首尾包含模糊（短侧≥2字）
  - InboundOrder 加 qmaiStoreId（按原计划补回）+ warehouseCode 字段；9.2.3 warehouseNo 优先用订单自身 warehouse_code → cangkuid → warehouseMark
  - QmaiClient：InboundOrderSummary 加 warehouseCode/warehouseId 解析；DeclareOrderSummary 加 purchaseNoList
- migration-add-inbound-order.sql：inbound_order 加 qmai_store_id + warehouse_code 列（幂等 DROP 重建，仍待用户执行）
- tmp_inbound_backfill.py 同款四级匹配 + 写 qmai_store_id/warehouse_code + cangkuid 缺失回填（名称匹配或报货单唯一仓库时，仅空时写）；重跑结果：**层级1 4303 + 层级2 1381 = 5682 单 / 35593 明细**；未匹配 421 全为冷冻仓 PSCK000149 / 新螺蛳湾总仓 PSCK000023 / 培训部报货专用店 MDCK000094（仓库级与培训部单，正确地排除）
- 待用户执行：① migration-add-inbound-order.sql ② tmp_inbound_backfill.sql ③ 打包部署 mp-server
