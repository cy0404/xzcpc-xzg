# 2026-08-28 未验收问题提醒：门店 chatId 过滤 + 单群触发 + 旧卡片撤回

## 背景

「未验收问题提醒」飞书卡片上线后，测试群打开 H5 显示**全部门店**问题 → 门店群卡片必须按群过滤；发现旧 jar 发的 49 张门店卡片按钮**无 chatId**（门店点开看全量）→ 全部撤回。

## 代码改动（已打包 server jar，13:31）

### IssueAcceptanceReminderJob.java（server/.../job/）

- 卡片链接 `/upload/h5/` → **`/h5/`**（飞书 H5 = jar 内 static/，与 loss-daily-confirm.html 一致；小程序 web-view H5 才走 `/upload/**`）
- 表格**删序号列**，columns = title(lark_md 链接跳 H5) / type / time 三列
- **测试群运行时切换**：`fms.getIssueReminderTestChatId()` 优先读 `sys_config.feishu_issue_reminder_test_chat_id`（改库即生效，无需重启），兜底 yml `issue-reminder-test-chat-id`
- **测试模式**（testChatId 非空）：固定发测试群，按钮**不带** chatId（H5 全量，测试用）
- **门店模式**（testChatId 为空）：按 `store_info.chat_id` 分组发，按钮带 `?chatId=` → H5 只显示本店问题；**无待验收问题的群不发卡片**（分组里就没有）
- 新增 `doSendToGroup(String chatId)`：单群触发，查该群对应门店待验收问题，空则跳过，按钮带 chatId

### LossReportH5Controller.java（server/.../controller/）

- `POST /api/public/issue/trigger-acceptance-reminder` 支持 **`?chatId=` 可选参数**：有值→单群（返回「已触发未验收问题提醒（单群 xxx）」）；无值→完整逻辑（测试群/全部门店群）
- `GET /api/public/issue/pending-list?chatId=xxx`：可选 chatId 过滤（JOIN store_info 按群查），不传全量
- **编译修复**：`outboundPrice(Map)` 方法缺失（本地未提交的「出库建单」代码引用了未定义方法 → 编译失败），按注释补上——`order_price` 优先、`purchase_price` 兜底，均无效返回 null（`toDecimal` 解析，signum>0 才算有效）

### static/h5/issue-accept.html

- **按钮复位修复**：已解决/未解决弹窗成功分支 + 下次打开时显式复位按钮（原来卡「提交中…」）
- **20 秒自动轮询** + 指纹 diff（`lastFp` = id 集合 join，未解决的问题仍在列表则不重渲染）
- `CHAT_ID` 从 URL 参数读取，带参只请求本店列表

## 飞书 API 踩坑（重要，详见 patterns.md P013）

- **撤回消息是 `DELETE /open-apis/im/v1/messages/{message_id}`**，POST 打过去网关 404「page not found」
- 撤回仅 **24 小时内**有效；必须用**发送方应用**的 tenant token
- 群消息列表 `im/v1/messages` 的 `start_time` **必须配 `end_time` 成对传**，单独传报 230001
- 生产应用 secret 明文在 `application-local.yml`（FEISHU_APP_SECRET，未提交 git），本机可直接调飞书 API

## 运维操作（已完成）

- 撤回 **49/49 张**旧版门店卡片（send_log ID 4-52，13:04-13:05 发）——`recall-issue-cards.sh`（本地脚本，已修正 DELETE 方法）+ `recall-ids.txt`
- 测试群 3 张卡片（ID 1-3）未动（无害）
- 生产探测验证：H5 200 + CHAT_ID 代码在；`pending-list` 带红河弥勒 chatId 只回 2 条；未知 chatId 空列表

## 待办 / 状态

- **新 jar（单群触发版）已打包未部署**（server 13:31 / mp-server 13:31）；生产当前是 13:16 部署的新版（chatId 过滤有、单群参数无）
- 用户要发全量门店群：`POST /api/public/issue/trigger-acceptance-reminder`（无参）——**触发前确认 sys_config.feishu_issue_reminder_test_chat_id 为空**，否则发测试群
- 每天早上 9:00 定时任务自动发（cron `0 0 9 * * ?`），同样受测试群配置控制
