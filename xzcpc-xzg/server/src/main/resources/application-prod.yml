server:
  port: 4026
  tomcat:
    max-http-form-post-size: 50MB

spring:
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB
  datasource:
    url: jdbc:mysql://162.14.122.80:3306/store_inventory?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&useSSL=false&connectTimeout=5000&socketTimeout=15000
    username: store_inventory
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      idle-timeout: 300000
      max-lifetime: 600000
      keepalive-time: 60000
      connection-timeout: 5000
      validation-timeout: 3000
      connection-test-query: SELECT 1

logging:
  level:
    com.xzcpc: INFO
    org.springframework.web: WARN
    root: WARN

# ---------- 飞书应用配置 ----------
feishu:
  app-id: cli_aa90f3d66d7adbb7
  app-secret: ${FEISHU_APP_SECRET}

# ---------- PostgreSQL 外部数据源（企迈采购/订货/消耗） ----------
pg:
  datasource:
    url: jdbc:postgresql://119.45.162.160:5433/tea_chain
    username: postgres
    password: ${PG_PASSWORD:xzcp123456}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 5
      minimum-idle: 1
      connection-timeout: 10000
      idle-timeout: 300000
      max-lifetime: 600000

# ---------- 总部端管理员 JWT ----------
app:
  public-url: https://www.xzcpc-9pd.top
  upload:
    path: /data/inventory-upload
  # 每日凌晨 3 点物料同步（xinfo），迁移完成后设置 APP_SYNC_ENABLED=true 开启
  sync:
    enabled: ${APP_SYNC_ENABLED:false}

admin:
  jwt:
    secret: ${ADMIN_JWT_SECRET}
    expire-hours: 72
  local-dev-login:
    enabled: false

# ---------- 企迈开放平台（server 依赖 mp 模块，需提供 QmaiClient 所需配置） ----------
qmai:
  open-id: 71fcea7abc9709d653693116410b5385
  grant-code: WaGI2rqy9b
  open-key: KYDwl5j39Wb71bff5fe6b0434ac6366a913c0dbc947q1SpQyZ
  base-url: https://openapi.qmai.cn
  console:
    base-url: https://inapi.qmai.cn
    cookie: ${QMAI_CONSOLE_COOKIE:}
    seller-id: ${QMAI_CONSOLE_SELLER_ID:216708}
