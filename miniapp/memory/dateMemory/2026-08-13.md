# 2026-08-13 无小程序端代码改动

今天改动全部在 H5 页面（xzcpc-xzg/upload/h5/，由 mp-server 静态服务），小程序 src 无代码变更。

## H5 到货验收报损（loss-arrival.html）

1. **牛油果泥厂家选择**：选中牛油果泥后展示「厂家」卡片（必填）：
   - hass牛油果（`guide-img/hass.jpg`）
   - 蓝蛙牛油果（`guide-img/lanwa.jpg`）
   - 两列图片卡片 `.mfr-item`，选中绿色高亮
2. **厂家存储方案**：不新增数据库字段，拼接进 `remark`：
   ```
   厂家：hass牛油果
   用户备注内容
   ```
   提交时自动拼接，resubmit 时从 remark 前缀解析还原
3. **牛油果泥隐藏加急**：选中牛油果泥隐藏「是否加急」卡片并重置为否；清空/换物料恢复
4. **上传数量 4→10**：三处改动（提示文案/校验 `>=10`/按钮显隐 `<10`）；单文件仍 200M；`voucher_url` VARCHAR(4000) 够用
5. **报损指引模块更新**：新增水印示例图（image12）+ 喷码示例图（image13）；三条要点：带时间+定位水印拍清喷码面 / 拍销毁视频剪开扔垃圾桶 / 每天报损勿积压；模块整体缩小（视频缩略图最高120px、文字12px）
6. **修复选物料后页面跳动**：搜索抽屉 `input.focus()` 引发微信 webview 滚动；打开抽屉记录 `_scrollY` 并锁 body 滚动，关闭时恢复（120ms 二次恢复防键盘收起）

## H5 操作指引书（loss-guide.html）

「一、报损步骤」新增：
- 图片必须带时间+定位水印（示例 image12.jpg）
- 图片需体现喷码那一面，上传原图（工厂按喷码溯源）（示例 image13.jpg）
- 视频：最好当晚结束营业统一剪开当天要报损的袋子拍销毁视频
- 时效：每天报损勿积压（避免跨年产品）

## 数据库

- `database/migration-add-qmai-store-id.sql`：生产库 `store_info` 缺 `qmai_store_id` 列（总部端 /api/reports/daily-loss 报 Unknown column），需执行 ALTER；数据填充见根目录 `tmp_qmai_store_sync.sql`

## 文档

- 完整变更说明：`docs/牛油果到货报损更新说明.md`

## 部署提醒

- 生产需同步：`loss-arrival.html`、`loss-guide.html`、`guide-img/image12.jpg`、`image13.jpg`、`hass.jpg`、`lanwa.jpg`
- 生产库执行 `database/migration-add-qmai-store-id.sql`
