# 2026-07-15 小程序端更新

## 首页/工具页门店选中记忆
- userStore 新增 selectedScope 字段（会话级，杀进程重置为 'all'）
- 首页与工具页共享同一选中门店：任一边切换，另一边 onShow 自动同步
- 带容错：若已存门店不在当前列表，回退到 'all'
- logout() 时重置为 'all'

## Tab 切换 loading 闪烁修复
- 首页 onShow 增加 dataReady 标记
- 首次加载正常显示 loading；后续 Tab 切回且 scope 未变时直接 return，不触发 API
- scope 变更（用户主动切门店）通过 watch 正常触发加载

## 老板首页清理
- 删除 pages/owner-home/ 目录 + pages.json 路由注册
- 死代码，无任何入口引用，不影响功能

## 按钮位置调整
- 报损列表操作按钮调换位置：拒绝/未收到货(红)在左，通过/已收货(绿)在右
- 与调货管理按钮风格统一（胶囊形）
