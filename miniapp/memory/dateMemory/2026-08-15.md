# 2026-08-15 智能订货测试部署

## 404 修复与部署
- 404 原因：小程序 ENV=test 打开的 `/test/storeInventory/upload/h5/smart-order-list.html` 在测试服务器不存在（对照 inbound-list 200）——文件没传 `/home/StoreInventory/test/mq-server/upload/h5/`
- 用户已传 H5 两文件 + 部署新 mp-server jar（探测均 200）
- 生产目录 `/data/inventory-upload/h5/` 缺 inbound-list/detail（切 prod 后入库入口会 404），上线时补传 4 个文件

## 测试门店种子数据
- `tmp_smart_order_seed_test_store.sql`（根目录）：给测试门店插 1 张 pending 建议单，suggest 为测试值
- 测试门店无盘点数据 → 明细三级降级：已提交任务汇总 → store_zone_material 分区清单（固定3）→ material 主数据前10条（固定3）
- 幂等（本周已有单据跳过）+ 带诊断/清理语句
- 真实生成 = 周一 6 点定时任务 或 手动 POST /api/mp/smart-order/generate（全门店、需 token）
- ⚠️ H5 确认订货会真实调企迈下单

## 库存单位与整数（用户拍板）
- 用户：单位换算成库存单位、价格也换算、不允许小数（仓库不好发货）
- 后端：suggest_qty 统一向上取整为整数（库存单位）；unit_price 换算为每库存单位单价（×换算系数）；确认接口拒绝小数
- H5 详情页：输入框 numeric + 取整，单价显示「¥xx/单位」；顶部「智能订货」标题已删
- 种子脚本：数量整数 + 单价按单级换算系数换算；已跑过旧种子的先执行清理段再重跑
- 8/15 诊断确认（tmp_smart_order_price_diag.py）：测试库单据仍是旧种子数据（qty 带小数、unit_price 全是基础单位价，如茶汤 L→g 应 3.80 实存 0.00），新种子被幂等跳过 → 处理 = 执行清理段 + 重跑种子 + 部署含换算改动的新 jar
- UI 微调（用户要求）：列表页概览面板改灰色 #EDEFED（非点击感）；详情页顶部返回按钮整体移除（用微信 web-view 自带导航返回）——两个文件都改 upload/h5，直接传测试服务器
